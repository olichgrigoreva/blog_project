Передача props ()

<R />
  class R {
    render(){
    <C1 number = "1">
    }
  }

class C1 {
  render (){
    <C2 number = {
      this.props.number />
    }
  }
}

class C2 {
 render (){
  <div> {this.props.number} </div>
 }
}

Все, что внутри state - к нему родитель тоже не имеет доступа (state - собственность КОМПОНЕНТА)
state нужен в компоненте для выполнения определенных действий, если компонент предназначен тольо для отображения, то state не нужен

Компонент (родитель) передает дочернему (с логикой обраборки) значение - через props, тот запоминает в состоянии (state) и докручивает полученное значение
до нужного и также до


БЛОГ
сущность - список постов
let blog = [
  {
    title;
    text;
    date;
  }
]


СКОБКИ
{возспользоваться значением переменной}
map - функция, метод map()
=> function (post){...}
